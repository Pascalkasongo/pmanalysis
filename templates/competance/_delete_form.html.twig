<form method="post" action="{{ path('app_competance_delete', {'id': competance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ competance.id) }}">
    <button class="btn">Delete</button>
</form>
